# Client engagement work — never commit to a public repo
engagements/

# Box write-ups — HTB/HackSmarter rules prohibit publishing write-ups for
# ACTIVE machines. Keep all lab write-ups local; never push to a public repo.
writeups/
kb/writeups/

# Secrets — used by skills/scripts/redact_engagement.py to wipe creds from
# the deliverable. Defense-in-depth: even though engagements/ is gitignored,
# explicitly deny .secrets.yaml at any depth in case someone moves one.
.secrets.yaml
**/.secrets.yaml

# Local scratch
.tmp-*/
*.log
.env

# Claude Code local state — agent worktrees, session scratch (not for the repo)
.claude/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
